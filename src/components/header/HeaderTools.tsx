import DarkModeButton from "../buttons/icons/DarkModeButton";
import GitHubButton from "../buttons/icons/GitHubButton";

const HeaderTools = () => {
  return (
    <div className="header-tools">
      <DarkModeButton />
      <GitHubButton />
    </div>
  );
}

export default HeaderTools